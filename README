Global setup:
 Download and install Git
  git config --global user.name "liuhb"
  git config --global user.email liuhb2@gmail.com
        Next steps:
  mkdir test
  cd test
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:liuhb/test.git
  git push -u origin master
      Existing Git Repo?
  cd existing_git_repo
  git remote add origin git@github.com:liuhb/test.git
  git push -u origin master
      Importing a Subversion Repo?
  Click here
      When you're done:
  Continue

